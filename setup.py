from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='The task is to forecast the total amount of products sold in every shop',
    author='yahiathen',
    license='',
)
